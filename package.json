{
  "name": "name-check",
  "version": "0.0.1",
  "description": "Generate all possible host names sequentially and check if they are used within a domain.",
  "main": "./dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Leif Kremkow <kremkow@tftg.net> (http://www.tftg.com)",
  "license": "AGPL-3.0-only",
  "private": true,
  "devDependencies": {
    "@types/node": "^8.10.59"
  },
  "dependencies": {
    "event-stream": "^4.0.1"
  }
}
